---
title: "Sprint 12 — Review (v2.20.0)"
description: "CrabPascal docs - Sprint 12 — Review (v2.20.0)"
---

# Sprint 12 — Review (v2.20.0)

**Fechamento:** 2026-05-26

## Entregas

- Property `read`/`write` por **método** funciona em `run` (VMT dispatch): `Value read GetValue write SetValue`.
- Semântica deixa de bloquear fields `F*` dentro de métodos `TClasse::Metodo` (para suportar backing fields).
- Novo fixture \+ teste: `tests/fixtures/property_accessor_methods.pas` \+ `tests/properties_runtime.rs`.

**Tag `v2.20.0` aprovada.**